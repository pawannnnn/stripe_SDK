const customers = require('../api/customer')
const invoices= require('../api/invoice')
const payments = require('../api/payments')
const subscriptions= require('../app/subscriptions')
module.exports={
  customers, invoices, payments ,subscriptions
  }
